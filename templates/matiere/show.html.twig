<!DOCTYPE html>

<title>Matiere</title>

{% block body %}
    <h1>Matiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Num_matiere</th>
                <td>{{ matiere.numMatiere }}</td>
            </tr>
            <tr>
                <th>Design</th>
                <td>{{ matiere.design }}</td>
            </tr>
            <tr>
                <th>Coef</th>
                <td>{{ matiere.coef }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_matiere_index') }}">back to list</a>

    <a href="{{ path('app_matiere_edit', {'id': matiere.id}) }}">edit</a>

    {{ include('matiere/_delete_form.html.twig') }}
{% endblock %}
